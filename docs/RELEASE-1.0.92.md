# KAMUCL 1.0.92

首页右下角标题更新为「参与测试及创作者 / TESTERS & CREATORS」。在原有 BAI_ZHU、AeZz、物晖后，按确认顺序追加牛肉、好好、hun_Bk、J39、略略略、鸦猫、sheri、书九叶、yansan、(x_x;)、小坎坷、MuxYang、八千代、Henry、月を見ていた、Hiro、摇滚高手，共 20 位。

鼠标移入卡片，名字依次平滑浮现；全部显示后，每个名字沿各自的随机方向在窗口内缓慢飘动，转向与启动都有平滑过渡。移开鼠标后淡出，气泡不拦截界面点击。支持键盘聚焦查看、Esc 收起；系统开启「减少动态效果」时静态显示名单。

窗口缩放时保持名字在可见范围内；页面卸载或窗口隐藏时停止动画并清理资源。沿用主题配色、卡片链接及已有个性化布局。

已验证名单顺序、依次浮现、独立飘动、窗口边界、快速移入移出、窄窗口、减少动态效果和页面切换清理；完成类型检查、生产构建及 Windows 便携包启动和 SHA256 校验。

本次仅交付 Windows x64，不生成 Mac 包。沿用现有未签名状态。
